# Python auto-detection via requirements.txt and runtime.txt
# Poppler required for pdf2image

[phases.setup]
nixPkgs = ["poppler_utils"]

[start]
cmd = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

